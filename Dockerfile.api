FROM python:3.11-slim

ENV PYTHONDONTWRITEBYTECODE=1 \
    PYTHONUNBUFFERED=1 \
    PIP_NO_CACHE_DIR=1

WORKDIR /app

COPY api/requirements.txt /tmp/api-requirements.txt
RUN pip install --no-cache-dir --requirement /tmp/api-requirements.txt \
    && useradd --create-home --uid 10001 api

COPY --chown=api:api api /app/api
COPY --chown=api:api activation_probe_mvp/__init__.py /app/activation_probe_mvp/__init__.py
COPY --chown=api:api activation_probe_mvp/probe_models.py /app/activation_probe_mvp/probe_models.py

USER api

CMD ["sh", "-c", "uvicorn api.main:app --host 0.0.0.0 --port ${PORT:-8000}"]
